{% extends 'base.html' %}

{% block title %}Login{% endblock %}

{% block content %}
  <div class="row">
    <h4>Login</h4>
    <div class="well page">
      <form method="post">
        {% csrf_token %}
        {{ form.as_p }}
        <button type="submit">Login</button>
      </form>
    </div>
  </div>
{% endblock %}
